package com.e.cryptocracy.utility;

import com.e.cryptocracy.R;
import com.google.android.material.dialog.MaterialAlertDialogBuilder;

public class AppFeedback {


    public void showFeedbackDialog() {
        /*MaterialAlertDialogBuilder(App.context,
                R.style.ThemeOverlay_MaterialComponents_MaterialAlertDialog_FullWidthButtons)
                .setMessage(App.context.getString(R.string.long_message))
                .setNegativeButton(resources.getString(R.string.decline)) {
            dialog, which ->
            // Respond to negative button press
        }.setPositiveButton(resources.getString(R.string.say_something)) {
            dialog, which ->
            // Respond to positive button press
        }
        .show()*/
    }
}
